ollama>=0.4.0
requests>=2.28.0

# Optional — Web search (recommended for reliable DuckDuckGo results)
ddgs>=9.0

# Optional — Markdown to PDF conversion (md2pdf skill, now built-in)
# markdown
# pymdown-extensions
# xhtml2pdf

# Optional — browser automation
# playwright
# playwright-stealth

# Optional — Google Calendar integration
# google-api-python-client
# google-auth-httplib2
# google-auth-oauthlib

# Optional — DOCX to Markdown conversion (docx2md skill)
# markitdown
