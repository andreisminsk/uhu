ollama>=0.4.0
requests>=2.28.0

# Recommended — rich multiline input (Shift+Enter, arrow keys, history)
prompt_toolkit>=3.0

# OpenAI-compatible API support (--api-openai flag and image-analysis tool)
openai>=1.0.0

# Browser automation packages (run `playwright install chromium` once to get the browser binary)
playwright
playwright-stealth

# Optional — Web search (recommended for reliable DuckDuckGo results)
ddgs>=9.0

# Optional — Markdown to PDF conversion (md2pdf skill, now built-in)
markdown
pymdown-extensions
xhtml2pdf

# Optional — Google Calendar integration
google-api-python-client
google-auth-httplib2
google-auth-oauthlib

# Optional — DOCX to Markdown conversion (docx2md skill)
markitdown

# Optional — Clipboard image paste (Ctrl+V image detection on Windows/macOS)
Pillow>=8.1.0

# Optional — System info tool (sys_info: RAM, CPU, GPU load, top processes)
psutil>=5.9.0

# Optional — Ollama balance indicator (native API, needs exported cookies)
beautifulsoup4>=4.12.0
